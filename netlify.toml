[build]
  publish = "dist"
  command = "echo $PRIVATE_KEY > private_key.pem && npx pnpm i --store=node_modules/.pnpm-store && npx pnpm run build"
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
